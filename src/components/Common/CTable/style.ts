import styled from "styled-components";

export const CTableContainer = styled.table`
  font-size: 12px;
`;

export const CTableScrollWrapperContainer = styled.div`
  overflow-y: scroll;

  ::-webkit-scrollbar {
    display: none;
  }
`;
